import React from 'react';
import './Header.css';

function Header(props) {
    return(
        <header className="Header">
            <h1>Header</h1>
        </header>
    );
}

export default Header;